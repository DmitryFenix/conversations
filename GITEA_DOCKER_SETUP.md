# Настройка Gitea в Docker (порт 4001)

## ✅ Конфигурация готова

Gitea настроен для работы в Docker контейнере:
- **Внутренний порт**: 4000 (для доступа из других контейнеров)
- **Внешний порт**: 4001 (для доступа из браузера)
- **URL для API**: `http://gitea:4000` (внутри Docker сети)
- **URL для браузера**: `http://localhost:4001`

## 🚀 Запуск

### 1. Запустите контейнеры

```bash
docker compose up -d
```

### 2. Откройте Gitea в браузере

Откройте: `http://localhost:4001`

### 3. Первоначальная настройка Gitea

1. **Заполните форму установки**:
   - **Database Type**: SQLite3 (по умолчанию)
   - **Site Title**: Code Review Platform
   - **Repository Root Path**: `/data/git/repositories`
   - **SSH Server Domain**: `localhost`
   - **SSH Port**: `2222`
   - **HTTP Port**: `4000` (внутренний порт, не меняйте!)
   - **Gitea Base URL**: `http://localhost:4001` (уже настроено)

2. **Создайте администратора**:
   - **Username**: `admin` (или любой другой)
   - **Password**: создайте надёжный пароль
   - **Email**: `admin@code-review.local`

3. **Нажмите "Install Gitea"**

### 4. Создайте API токен

1. **Войдите в Gitea** с учётными данными администратора

2. **Перейдите в настройки**:
   - Нажмите на аватар → **Settings** → **Applications** → **Generate New Token**

3. **Создайте токен**:
   - **Token Name**: `code-review-platform-admin`
   - **Select scopes**: выберите **Select all** или **Full control of Gitea**
   - **Generate Token**

4. **Скопируйте токен** (показывается только один раз!)

### 5. Установите токен

Создайте файл `.env` в корне проекта:

```bash
GITEA_ADMIN_TOKEN=ваш_скопированный_токен_здесь
```

**Важно**: Добавьте `.env` в `.gitignore`!

### 6. Перезапустите API

```bash
docker compose restart api
```

### 7. Проверьте логи

```bash
docker compose logs api | grep -i gitea
```

Должно появиться:
```
INFO:main:Gitea client initialized: http://gitea:4000
```

## ✅ Проверка интеграции

### 1. Создайте тестовую сессию

1. Откройте `http://localhost:8000/reviewer`
2. Нажмите "Создать сессию"
3. Введите имя кандидата (например, "Тест Тестов")
4. Нажмите "Создать"

### 2. Проверьте ответ

В ответе должно быть поле `gitea`:

```json
{
  "session_id": 1,
  "access_token": "...",
  "gitea": {
    "enabled": true,
    "user": "candidate_тест_тестов",
    "repo": "session_1",
    "web_url": "http://localhost:4001/candidate_тест_тестов/session_1"
  }
}
```

### 3. Проверьте в Gitea

1. Откройте `http://localhost:4001`
2. Войдите как администратор
3. Перейдите в **Site Administration** → **Users**
4. Найдите пользователя `candidate_тест_тестов`
5. Перейдите в его репозиторий `session_1`

## 🎯 Что должно появиться в интерфейсе

### Reviewer Dashboard

После создания сессии с Gitea:
- Секция **"Gitea Integration"** с информацией о репозитории
- Ссылка на репозиторий: `http://localhost:4001/user/repo`
- Кнопка **"Создать PR"** (после создания PR)
- Кнопки **"Обновить PR"** и **"Синхронизировать комментарии"**

### Candidate View

- Кнопка **"Репозиторий"** в header
- Ссылка ведёт на `http://localhost:4001/user/repo`

## 📋 Управление

### Остановить все сервисы

```bash
docker compose down
```

### Остановить только Gitea

```bash
docker compose stop gitea
```

### Перезапустить Gitea

```bash
docker compose restart gitea
```

### Посмотреть логи Gitea

```bash
docker compose logs gitea
```

### Посмотреть статус

```bash
docker compose ps
```

## 🔧 Настройки

### Изменить порт

Если нужно использовать другой внешний порт (например, 5000):

1. Обновите `docker-compose.yml`:
   ```yaml
   ports:
     - "5000:4000"  # Внешний порт 5000
   environment:
     - GITEA__server__ROOT_URL=http://localhost:5000
   ```

2. Обновите `.env`:
   ```bash
   GITEA_WEB_URL=http://localhost:5000
   ```

3. Перезапустите:
   ```bash
   docker compose restart gitea api
   ```

### Доступ к данным Gitea

Данные Gitea хранятся в `./gitea_data`:
- База данных: `./gitea_data/gitea/gitea.db`
- Репозитории: `./gitea_data/git/repositories`
- Конфигурация: `./gitea_config/app.ini`

## 🎉 Готово!

Теперь Gitea работает в Docker и полностью интегрирован с приложением.

