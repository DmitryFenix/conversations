# Workflow для кандидата в Gitea

## 🎯 Цель сессии

Кандидат должен продемонстрировать навыки **code review**:
- Умение находить проблемы в коде
- Корректность комментариев (тип проблемы, серьёзность)
- Качество описания найденных проблем

## 📋 Что должен делать кандидат в Gitea

### 1. **Просмотр кода в Pull Request**
   - Открыть PR в Gitea (автоматический редирект при переходе по ссылке)
   - Просмотреть код в PR
   - Изучить изменения (diff между ветками)

### 2. **Поиск проблем в коде**
   Кандидат должен найти проблемы следующих типов:
   - **Bug** - ошибки в логике, потенциальные падения
   - **Security** - уязвимости безопасности
   - **Style** - проблемы стиля кода, читаемости
   - **Performance** - проблемы производительности

### 3. **Оставление комментариев**
   Для каждой найденной проблемы кандидат должен:
   - Выбрать правильный **тип** проблемы (bug/security/style/performance)
   - Указать правильную **серьёзность** (critical/high/medium/low)
   - Написать понятное **описание** проблемы
   - Указать **файл** и **диапазон строк**

### 4. **Что НЕ нужно делать**
   ❌ **НЕ нужно исправлять код** - это не задача code review
   ❌ **НЕ нужно создавать коммиты** - только комментирование
   ❌ **НЕ нужно пушить изменения** - только просмотр и комментарии

## 🔍 Как оценивается работа

Система сравнивает комментарии кандидата с `golden_truth.json`:
- **True Positives (TP)** - правильно найденные проблемы
- **False Positives (FP)** - неверно указанные проблемы
- **False Negatives (FN)** - пропущенные проблемы

**Оценка:**
- `score = TP / (TP + FP + FN)`
- `Junior` - если score < 0.45
- `Middle` - если score < 0.70
- `Senior` - если score >= 0.70

## 💡 Пример правильного комментария

```json
{
  "file": "main.py",
  "line_range": "10-12",
  "type": "security",
  "severity": "high",
  "text": "Hardcoded JWT secret key - should use environment variables"
}
```

## ⚠️ Проблема текущей реализации

Сейчас кандидат имеет **полные права** на репозиторий (владелец), что позволяет:
- Редактировать код
- Создавать коммиты
- Пушить изменения

**Это не нужно для code review!** Кандидат должен только **комментировать**, а не исправлять.

## 🔧 Рекомендации

### Вариант 1: Ограничить права кандидата (рекомендуется)
- Создавать репозиторий от имени ревьюера (или системного пользователя)
- Давать кандидату права только на **чтение и комментирование** в PR
- Кандидат не сможет редактировать код, только комментировать

### Вариант 2: Оставить как есть, но добавить инструкции
- Добавить чёткие инструкции в интерфейс Gitea
- Указать, что нужно только комментировать, не исправлять
- Оставить возможность редактирования для будущих улучшений

### Вариант 3: Использовать только комментарии в PR
- Кандидат работает только через интерфейс комментариев PR
- Не давать доступ к редактированию файлов
- Все комментарии синхронизируются с нашей системой

## 📝 Текущий workflow

1. **Ревьюер создаёт сессию** → создаётся репозиторий с начальным кодом
2. **Создаётся PR** из ветки `candidate-work-{id}` в `main`
3. **Кандидат открывает PR** → видит код
4. **Кандидат комментирует** → находит проблемы и оставляет комментарии
5. **Ревьюер оценивает** → система сравнивает комментарии с golden_truth

## 🎯 Идеальный workflow для code review

1. Кандидат получает ссылку на PR
2. Кандидат просматривает код в PR
3. Кандидат оставляет комментарии к проблемным местам
4. Комментарии синхронизируются с нашей системой
5. Система оценивает качество комментариев

**Ключевой момент:** Кандидат **НЕ должен** редактировать код, только **комментировать**!

