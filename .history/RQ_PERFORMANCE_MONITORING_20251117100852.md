# Мониторинг эффективности асинхронного модуля

Система мониторинга производительности RQ задач позволяет отслеживать изменения эффективности работы асинхронного модуля во времени.

## Веб-интерфейс мониторинга (RQ Dashboard)

Для визуального мониторинга RQ задач доступен официальный веб-интерфейс **RQ Dashboard**.

### Доступ

После запуска Docker Compose, RQ Dashboard доступен по адресу:
- **http://localhost:9181**

### Возможности

RQ Dashboard предоставляет:
- 📊 **Визуализацию очередей** - статус всех очередей в реальном времени
- 📋 **Список задач** - все задачи с их статусами (queued, started, finished, failed)
- ⏱️ **Время выполнения** - длительность выполнения задач
- 🔍 **Детали задач** - полная информация о каждой задаче
- 📈 **Статистика** - количество задач по статусам
- 🔄 **Автообновление** - автоматическое обновление данных

### Запуск

⚠️ **Важно**: RQ Dashboard - опциональный сервис. В продакшене он отключён по умолчанию.

RQ Dashboard запускается только с профилем `monitoring`:

```bash
# Запустить все сервисы ВКЛЮЧАЯ RQ Dashboard (для разработки/мониторинга)
docker compose --profile monitoring up -d

# Или только RQ Dashboard
docker compose --profile monitoring up -d rq-dashboard
```

### Запуск БЕЗ Dashboard (продакшен)

```bash
# Запустить все сервисы БЕЗ RQ Dashboard (по умолчанию)
docker compose up -d
```

RQ Dashboard не будет запущен, так как он в профиле `monitoring`.

### Настройка

RQ Dashboard автоматически подключается к Redis и отображает все очереди RQ. Дополнительная настройка не требуется.

---

## Что такое "тренды по периодам"?

**Тренды по периодам** - это анализ изменения метрик производительности во времени, разбитый на несколько временных интервалов (периодов).

### Пример

Представьте, что вы хотите понять, как изменилась производительность системы за последние 24 часа. Вместо одного общего показателя, тренды разбивают это время на несколько периодов:

```
Период 1: 0-4 часа назад    → 25 задач, среднее время 45 сек
Период 2: 4-8 часов назад   → 30 задач, среднее время 42 сек
Период 3: 8-12 часов назад  → 28 задач, среднее время 40 сек
Период 4: 12-16 часов назад → 35 задач, среднее время 38 сек
Период 5: 16-20 часов назад → 32 задачи, среднее время 35 сек
Период 6: 20-24 часа назад  → 40 задач, среднее время 33 сек
```

### Что это даёт?

1. **Выявление трендов**: Видно, что время выполнения уменьшается (45 → 33 сек) - система работает быстрее
2. **Обнаружение проблем**: Если в каком-то периоде метрики резко ухудшились, это видно сразу
3. **Сравнение периодов**: Можно сравнить "сегодня утром" с "вчера утром"
4. **Прогнозирование**: Понимание трендов помогает предсказать будущее поведение

### Метрики в трендах

Для каждого периода вычисляются:
- **Количество задач** - сколько задач было обработано
- **Процент успешных** - сколько задач завершилось успешно
- **Среднее время выполнения** - как долго выполнялись задачи
- **Пропускная способность** - сколько задач в час

### Изменения между периодами

Система автоматически вычисляет изменения:
- **duration_change_percent** - на сколько процентов изменилось время выполнения
- **throughput_change_percent** - на сколько процентов изменилась пропускная способность
- **success_rate_change** - на сколько процентов изменился процент успешных задач
- **is_improving** - флаг улучшения (true если время уменьшилось и успешность не ухудшилась)

### Пример интерпретации

```json
{
  "trends": {
    "duration_change_percent": -7.0,      // Время выполнения уменьшилось на 7%
    "throughput_change_percent": 20.0,    // Пропускная способность выросла на 20%
    "success_rate_change": 0.67,          // Процент успешных задач вырос на 0.67%
    "is_improving": true                   // ✅ Система улучшается!
  }
}
```

Это означает, что последний период лучше предыдущего: задачи выполняются быстрее, система обрабатывает больше задач, и процент успешных задач вырос.

---

## API Endpoints для мониторинга

Помимо веб-интерфейса RQ Dashboard, доступны REST API endpoints для программного доступа к метрикам:

### 1. Метрики производительности

**GET** `/api/rq/performance?hours=24`

Получить метрики производительности за последние N часов.

**Параметры:**
- `hours` (int, по умолчанию 24) - количество часов для анализа

**Пример ответа:**
```json
{
  "status": "ok",
  "metrics": {
    "period_hours": 24,
    "total_jobs": 150,
    "successful_jobs": 145,
    "failed_jobs": 5,
    "success_rate": 96.67,
    "throughput_per_hour": 6.25,
    "duration_metrics": {
      "avg_seconds": 45.32,
      "median_seconds": 42.15,
      "min_seconds": 12.5,
      "max_seconds": 120.8
    },
    "queue_time_metrics": {
      "avg_seconds": 2.3,
      "max_seconds": 15.7
    },
    "timestamp": "2025-11-17T00:00:00Z"
  }
}
```

**Метрики:**
- `total_jobs` - общее количество задач
- `successful_jobs` - количество успешных задач
- `failed_jobs` - количество неудачных задач
- `success_rate` - процент успешных задач
- `throughput_per_hour` - пропускная способность (задач в час)
- `duration_metrics` - метрики времени выполнения (среднее, медиана, мин, макс)
- `queue_time_metrics` - метрики времени ожидания в очереди

### 2. Тренды производительности

**GET** `/api/rq/performance/trends?periods=6&hours_per_period=4`

Получить тренды производительности за несколько периодов.

**Параметры:**
- `periods` (int, по умолчанию 6) - количество периодов для анализа
- `hours_per_period` (int, по умолчанию 4) - количество часов в каждом периоде

**Пример ответа:**
```json
{
  "status": "ok",
  "trends": {
    "periods": [
      {
        "period": 1,
        "hours_range": "0-4",
        "total_jobs": 25,
        "successful_jobs": 24,
        "failed_jobs": 1,
        "success_rate": 96.0,
        "avg_duration": 45.32,
        "throughput": 6.25
      },
      {
        "period": 2,
        "hours_range": "4-8",
        "total_jobs": 30,
        "successful_jobs": 29,
        "failed_jobs": 1,
        "success_rate": 96.67,
        "avg_duration": 42.15,
        "throughput": 7.5
      }
    ],
    "trends": {
      "duration_change_percent": -7.0,
      "throughput_change_percent": 20.0,
      "success_rate_change": 0.67,
      "is_improving": true
    },
    "summary": {
      "latest_avg_duration": 45.32,
      "latest_throughput": 6.25,
      "latest_success_rate": 96.0
    }
  }
}
```

**Тренды:**
- `duration_change_percent` - изменение времени выполнения в процентах
- `throughput_change_percent` - изменение пропускной способности в процентах
- `success_rate_change` - изменение процента успешных задач
- `is_improving` - флаг улучшения (true если время выполнения уменьшилось и процент успешных задач не ухудшился)

### 3. Сравнение эффективности

**GET** `/api/rq/performance/compare?current_hours=1&previous_hours=1`

Сравнить эффективность текущего периода с предыдущим.

**Параметры:**
- `current_hours` (int, по умолчанию 1) - количество часов для текущего периода
- `previous_hours` (int, по умолчанию 1) - количество часов для предыдущего периода

**Пример ответа:**
```json
{
  "status": "ok",
  "comparison": {
    "current_period": {
      "hours": 1,
      "total_jobs": 10,
      "successful_jobs": 9,
      "failed_jobs": 1,
      "success_rate": 90.0,
      "avg_duration_seconds": 42.5,
      "throughput_per_hour": 10.0
    },
    "previous_period": {
      "hours": 1,
      "total_jobs": 8,
      "successful_jobs": 8,
      "failed_jobs": 0,
      "success_rate": 100.0,
      "avg_duration_seconds": 45.2,
      "throughput_per_hour": 8.0
    },
    "changes": {
      "duration_change_seconds": -2.7,
      "duration_change_percent": -5.97,
      "throughput_change": 2.0,
      "throughput_change_percent": 25.0,
      "success_rate_change": -10.0,
      "is_improving": false
    },
    "timestamp": "2025-11-17T00:00:00Z"
  }
}
```

**Изменения:**
- `duration_change_seconds` - изменение времени выполнения в секундах
- `duration_change_percent` - изменение времени выполнения в процентах
- `throughput_change` - изменение пропускной способности
- `throughput_change_percent` - изменение пропускной способности в процентах
- `success_rate_change` - изменение процента успешных задач
- `is_improving` - флаг улучшения

## Использование

### Мониторинг в реальном времени

```bash
# Получить метрики за последний час
curl http://localhost:8000/api/rq/performance?hours=1

# Получить тренды за последние 24 часа (6 периодов по 4 часа)
curl http://localhost:8000/api/rq/performance/trends?periods=6&hours_per_period=4

# Сравнить последний час с предыдущим
curl http://localhost:8000/api/rq/performance/compare?current_hours=1&previous_hours=1
```

### Автоматический мониторинг

Можно настроить периодический сбор метрик для отслеживания трендов:

```python
import requests
import time
from datetime import datetime

def monitor_performance():
    while True:
        # Получаем метрики
        response = requests.get("http://localhost:8000/api/rq/performance?hours=1")
        metrics = response.json()["metrics"]
        
        # Получаем сравнение
        comparison = requests.get("http://localhost:8000/api/rq/performance/compare?current_hours=1&previous_hours=1")
        changes = comparison.json()["comparison"]["changes"]
        
        # Логируем изменения
        print(f"[{datetime.now()}] Performance:")
        print(f"  Duration: {changes['duration_change_percent']:.2f}%")
        print(f"  Throughput: {changes['throughput_change_percent']:.2f}%")
        print(f"  Success Rate: {changes['success_rate_change']:.2f}%")
        print(f"  Is Improving: {changes['is_improving']}")
        
        time.sleep(300)  # Каждые 5 минут
```

## Интерпретация метрик

### Время выполнения (duration)
- **Уменьшение** - хорошо, задачи выполняются быстрее
- **Увеличение** - может указывать на проблемы с производительностью

### Пропускная способность (throughput)
- **Увеличение** - хорошо, система обрабатывает больше задач
- **Уменьшение** - может указывать на узкие места

### Процент успешных задач (success_rate)
- **Высокий (>95%)** - хорошо, система стабильна
- **Низкий (<90%)** - требует внимания, возможны проблемы

### Время ожидания в очереди (queue_time)
- **Низкое (<5 секунд)** - хорошо, задачи быстро начинают выполняться
- **Высокое (>30 секунд)** - может указывать на перегрузку системы

## Рекомендации

1. **Регулярный мониторинг**: Проверяйте метрики каждые несколько часов
2. **Анализ трендов**: Используйте `/performance/trends` для выявления долгосрочных трендов
3. **Сравнение периодов**: Используйте `/performance/compare` для быстрого сравнения эффективности
4. **Алерты**: Настройте алерты при ухудшении метрик (например, success_rate < 90%)


