# Быстрая настройка Gitea интеграции

## ⚠️ Важно: Gitea требует первоначальной настройки

Сейчас в логах видно:
```
api-1     | INFO:main:Gitea client not initialized (no admin token)
gitea     | [I] Prepare to run install page
```

Это означает, что Gitea ещё не настроен. Следуйте инструкциям ниже.

## 🚀 Пошаговая настройка

### Шаг 1: Настройте Gitea через веб-интерфейс

1. **Откройте Gitea в браузере**: `http://localhost:3000`

2. **Заполните форму установки**:
   - **Database Type**: оставьте `SQLite3` (по умолчанию)
   - **Site Title**: `Code Review Platform`
   - **Repository Root Path**: `/data/git/repositories`
   - **SSH Server Domain**: `localhost`
   - **SSH Port**: `2222`
   - **HTTP Port**: `3000`
   - **Gitea Base URL**: `http://localhost:3000`

3. **Создайте администратора**:
   - **Username**: `admin` (или любой другой)
   - **Password**: создайте надёжный пароль (запомните его!)
   - **Email**: `admin@code-review.local`

4. **Нажмите "Install Gitea"**

### Шаг 2: Создайте API токен

1. **Войдите в Gitea** с учётными данными администратора

2. **Перейдите в настройки**:
   - Нажмите на аватар в правом верхнем углу
   - Выберите **Settings** → **Applications** → **Generate New Token**

3. **Создайте токен**:
   - **Token Name**: `code-review-platform-admin`
   - **Select scopes**: выберите **Select all** (или просто **Full control of Gitea**)
   - **Generate Token**

4. **Скопируйте токен** (он показывается только один раз!)

### Шаг 3: Установите токен в приложение

#### Вариант A: Через переменную окружения (рекомендуется)

Создайте файл `.env` в корне проекта:

```bash
GITEA_ADMIN_TOKEN=ваш_скопированный_токен_здесь
```

**Важно**: Добавьте `.env` в `.gitignore`, чтобы не коммитить токен!

#### Вариант B: Через docker-compose.yml (не рекомендуется для production)

Раскомментируйте и отредактируйте в `docker-compose.yml`:

```yaml
api:
  environment:
    - GITEA_ADMIN_TOKEN=ваш_токен_здесь
```

### Шаг 4: Перезапустите API

```bash
docker compose restart api
```

### Шаг 5: Проверьте логи

```bash
docker compose logs api | grep -i gitea
```

Должно появиться:
```
INFO:main:Gitea client initialized: http://gitea:3000
```

Если видите это сообщение — интеграция работает! ✅

## ✅ Проверка интеграции

### 1. Создайте тестовую сессию

1. Откройте `http://localhost:8000/reviewer`
2. Нажмите "Создать сессию"
3. Введите имя кандидата (например, "Тест Тестов")
4. Нажмите "Создать"

### 2. Проверьте ответ

В браузере (DevTools → Network) или в логах API должен появиться ответ с полем `gitea`:

```json
{
  "session_id": 33,
  "access_token": "...",
  "gitea": {
    "enabled": true,
    "user": "candidate_тест_тестов",
    "repo": "session_33",
    "web_url": "http://localhost:3000/candidate_тест_тестов/session_33"
  }
}
```

### 3. Проверьте в Gitea

1. Откройте `http://localhost:3000`
2. Войдите как администратор
3. Перейдите в **Site Administration** → **Users**
4. Найдите пользователя `candidate_тест_тестов`
5. Перейдите в его репозиторий `session_33`

## 🎯 Что должно появиться в интерфейсе

После успешной настройки:

### Reviewer Dashboard
- В деталях сессии появится секция **"Gitea Integration"**
- Будет показан пользователь, репозиторий и ссылка на репозиторий
- Появится кнопка **"Создать PR"**

### Candidate View
- В header появится кнопка **"Репозиторий"** (синяя кнопка с иконкой GitBranch)
- Кнопка ведёт на репозиторий кандидата в Gitea

## ❌ Если что-то не работает

### Проблема: "Gitea client not initialized (no admin token)"

**Решение**: Проверьте, что токен установлен:
```bash
docker compose exec api env | grep GITEA_ADMIN_TOKEN
```

Если пусто, установите токен через `.env` файл и перезапустите:
```bash
docker compose restart api
```

### Проблема: "HTTP Error for POST /api/v1/admin/users: 401"

**Решение**: Токен неверный или истёк. Создайте новый токен в Gitea.

### Проблема: "Gitea not enabled for this session"

**Решение**: Сессия была создана до настройки Gitea. Создайте новую сессию после настройки токена.

## 📋 Краткая справка

```bash
# Проверить статус Gitea
docker compose ps gitea

# Посмотреть логи Gitea
docker compose logs gitea

# Посмотреть логи API (Gitea сообщения)
docker compose logs api | grep -i gitea

# Перезапустить только API
docker compose restart api

# Перезапустить всё
docker compose restart
```

## 🎉 После настройки

Интеграция Gitea позволяет:

1. ✅ Автоматически создавать пользователей для кандидатов
2. ✅ Создавать репозитории для каждой сессии
3. ✅ Инициализировать стартовый код
4. ✅ Создавать Pull Requests из интерфейса
5. ✅ Синхронизировать комментарии в Gitea PR
6. ✅ Кандидат может работать с кодом через Gitea UI

