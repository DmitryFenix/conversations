# Диагностика интеграции Gitea

## 🔍 Чеклист проверки

### 1. Проверьте, что Gitea запущен

```bash
docker compose ps
```

Должен быть контейнер `gitea` в статусе `Up`.

### 2. Проверьте, что Gitea доступен

Откройте в браузере: `http://localhost:4001`

Если не открывается:
- Проверьте логи: `docker compose logs gitea`
- Убедитесь, что Gitea настроен (первоначальная установка)

### 3. Проверьте, что Gitea настроен

Gitea должен быть настроен через веб-интерфейс:
- Создан администратор
- Завершена первоначальная установка

### 4. Проверьте наличие токена

Создайте файл `.env` в корне проекта:

```bash
GITEA_ADMIN_TOKEN=ваш_токен_здесь
```

**Как получить токен:**
1. Войдите в Gitea: `http://localhost:4001`
2. Settings → Applications → Generate New Token
3. Создайте токен с полными правами
4. Скопируйте токен

### 5. Проверьте логи API

```bash
docker compose logs api | grep -i gitea
```

**Должно быть:**
```
INFO:main:Gitea client initialized: http://gitea:4000
```

**Если видите:**
```
INFO:main:Gitea client not initialized (no admin token)
```
→ Токен не установлен или не загружен

**Если видите ошибку:**
```
WARNING:Failed to initialize Gitea client: ...
```
→ Проверьте, что Gitea доступен и токен правильный

### 6. Перезапустите API после установки токена

```bash
docker compose restart api
```

### 7. Создайте НОВУЮ сессию

**Важно:** Интеграция Gitea работает только для сессий, созданных ПОСЛЕ настройки Gitea.

1. Откройте `http://localhost:8000/reviewer`
2. Нажмите "Создать сессию"
3. Введите имя кандидата
4. Нажмите "Создать"

### 8. Проверьте ответ API

В браузере (DevTools → Network) проверьте ответ на `POST /api/reviewer/sessions`.

**Должно быть поле `gitea`:**
```json
{
  "session_id": 1,
  "access_token": "...",
  "gitea": {
    "enabled": true,
    "user": "candidate_...",
    "repo": "session_1",
    "web_url": "http://localhost:4001/..."
  }
}
```

**Если поля `gitea` нет:**
- Gitea клиент не инициализирован (нет токена)
- Или произошла ошибка при создании пользователя/репозитория

### 9. Проверьте логи при создании сессии

```bash
docker compose logs api | tail -50
```

Ищите сообщения:
- `Created Gitea user: ...`
- `Created Gitea repository: ...`
- `Initialized starting code in repository`
- Или ошибки: `Failed to create Gitea user`, `Error integrating with Gitea`

### 10. Проверьте интерфейс

После создания сессии с Gitea:
1. Откройте детали сессии в Reviewer Dashboard
2. Должна появиться секция **"Gitea Integration"** (синяя рамка)

**Если секции нет:**
- Сессия была создана без Gitea (старая сессия или ошибка)
- Создайте новую сессию после настройки Gitea

## 🐛 Частые проблемы

### Проблема: "Gitea client not initialized (no admin token)"

**Решение:**
1. Создайте файл `.env` в корне проекта
2. Добавьте: `GITEA_ADMIN_TOKEN=ваш_токен`
3. Перезапустите API: `docker compose restart api`

### Проблема: "Failed to initialize Gitea client: Connection refused"

**Решение:**
1. Проверьте, что Gitea запущен: `docker compose ps`
2. Проверьте логи Gitea: `docker compose logs gitea`
3. Убедитесь, что Gitea настроен (первоначальная установка)

### Проблема: "Failed to create Gitea user: 401"

**Решение:**
1. Проверьте, что токен правильный
2. Убедитесь, что токен имеет права администратора
3. Создайте новый токен в Gitea

### Проблема: Секция Gitea не отображается в интерфейсе

**Решение:**
1. Убедитесь, что создали НОВУЮ сессию после настройки Gitea
2. Проверьте ответ API - должно быть поле `gitea` с `enabled: true`
3. Обновите страницу в браузере

### Проблема: Gitea не открывается на localhost:4001

**Решение:**
1. Проверьте, что контейнер запущен: `docker compose ps`
2. Проверьте логи: `docker compose logs gitea`
3. Убедитесь, что порт 4001 не занят другим процессом

## ✅ Быстрая проверка

Выполните эти команды по порядку:

```bash
# 1. Проверьте статус контейнеров
docker compose ps

# 2. Проверьте логи API (Gitea)
docker compose logs api | grep -i gitea

# 3. Проверьте логи Gitea
docker compose logs gitea | tail -20

# 4. Проверьте наличие .env файла
cat .env | grep GITEA_ADMIN_TOKEN

# 5. Перезапустите API (если изменили .env)
docker compose restart api

# 6. Проверьте логи после перезапуска
docker compose logs api | grep -i gitea
```

## 📋 Итоговый чеклист

- [ ] Gitea контейнер запущен (`docker compose ps`)
- [ ] Gitea доступен в браузере (`http://localhost:4001`)
- [ ] Gitea настроен (первоначальная установка завершена)
- [ ] Создан API токен в Gitea
- [ ] Файл `.env` создан с `GITEA_ADMIN_TOKEN`
- [ ] API перезапущен после создания `.env`
- [ ] В логах API: `Gitea client initialized`
- [ ] Создана НОВАЯ сессия после настройки
- [ ] В ответе API есть поле `gitea` с `enabled: true`
- [ ] В интерфейсе отображается секция "Gitea Integration"

Если все пункты выполнены, но интеграция не работает - проверьте логи при создании сессии на наличие ошибок.

