# Исправление ошибки развертывания на Railway

## Проблема
Railway не поддерживает `VOLUME` в Dockerfile. Ошибка:
```
The `VOLUME` keyword is banned in Dockerfiles. Use Railway volumes instead.
```

## Решение

### ✅ Исправлено в Dockerfile

Удалена строка:
```dockerfile
VOLUME /artifacts /mr_packages
```

Теперь только:
```dockerfile
RUN mkdir -p /artifacts /mr_packages
```

### Что делать дальше:

1. **Закоммитьте изменения:**
```bash
git add api/Dockerfile
git commit -m "Fix: Remove VOLUME from Dockerfile for Railway compatibility"
git push
```

2. **Railway автоматически пересоберет проект:**
   - После пуша Railway обнаружит изменения
   - Начнет новую сборку автоматически
   - На этот раз должно пройти успешно

3. **Проверьте статус:**
   - Зайдите в Railway Dashboard
   - Дождитесь завершения сборки (3-6 минут)
   - Приложение должно запуститься

---

## Примечание о Volumes

Для демо-версии volumes не нужны — приложение будет работать.

Для production можно добавить Railway Volumes через настройки проекта:
- Settings → Volumes → Add Volume
- Mount point: `/artifacts` и `/mr_packages`

Но для начала это не обязательно!

---

*После пуша Railway автоматически пересоберет проект.* 🚀

