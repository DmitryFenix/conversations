# Что изменилось - Разделение ролей и интеграция с Gitea

## ✅ Реализованные изменения

### 1. Разделение ролей (Reviewer и Candidate)

**Было:**
- Один интерфейс для всех
- Не было разделения прав доступа

**Стало:**
- **Reviewer (Проверяющий)** - отдельный интерфейс на `/reviewer`
- **Candidate (Кандидат)** - отдельный интерфейс на `/candidate/:token`
- Чёткое разделение прав доступа

### 2. Новые URL и интерфейсы

#### Reviewer Dashboard (`/reviewer`)
- **Новый интерфейс для проверяющего**
- Список всех сессий
- Создание новых сессий с указанием имени кандидата
- Просмотр деталей сессии
- Запуск оценки
- Просмотр отчётов (текст + PDF)
- Продление сессии на 30 минут
- Просмотр токена кандидата и ссылки

**Как использовать:**
1. Откройте: `http://localhost:8000/reviewer`
2. Нажмите "Создать сессию"
3. Введите имя кандидата (например, "Иван Иванов")
4. Нажмите "Создать"
5. Получите токен и ссылку для кандидата

#### Candidate View (`/candidate/:token`)
- **Новый интерфейс для кандидата**
- Доступ только по уникальному токену
- Просмотр diff кода
- Добавление комментариев
- Таймер сессии (без возможности продления)
- **БЕЗ** отчётов и продления сессии

**Как использовать:**
1. Получите токен от Reviewer (после создания сессии)
2. Откройте: `http://localhost:8000/candidate/{token}`
3. Просматривайте код и добавляйте комментарии

### 3. Новые API Endpoints

#### Reviewer API (`/api/reviewer/*`)
- `POST /api/reviewer/sessions` - Создать сессию
- `GET /api/reviewer/sessions` - Список сессий
- `GET /api/reviewer/sessions/{id}` - Детали сессии
- `POST /api/reviewer/sessions/{id}/evaluate` - Запустить оценку
- `POST /api/reviewer/sessions/{id}/extend` - Продлить сессию
- `GET /api/reviewer/sessions/{id}/report` - Текстовый отчёт
- `GET /api/reviewer/sessions/{id}/report/pdf` - PDF отчёт

#### Candidate API (`/api/candidate/*`)
- `GET /api/candidate/sessions/{token}` - Получить сессию по токену
- `GET /api/candidate/sessions/{token}/diff` - Получить diff
- `GET /api/candidate/sessions/{token}/comments` - Получить комментарии
- `POST /api/candidate/sessions/{token}/comments` - Добавить комментарий

### 4. Интеграция с Gitea

**Что добавлено:**
- Gitea сервер в Docker Compose
- GiteaClient класс для работы с Gitea REST API
- Автоматическое создание пользователя для кандидата
- Автоматическое создание репозитория для сессии
- Инициализация стартового кода в репозитории

**Как использовать:**
1. Настройте Gitea (см. `GITEA_SETUP.md`)
2. Создайте API токен администратора
3. Установите `GITEA_ADMIN_TOKEN` в переменные окружения
4. При создании сессии автоматически создастся:
   - Пользователь в Gitea: `candidate_ivan_ivanov`
   - Репозиторий: `session_1`
   - Стартовый код в репозитории

**Информация о Gitea в ответе API:**
```json
{
  "session_id": 1,
  "access_token": "...",
  "gitea": {
    "enabled": true,
    "user": "candidate_ivan_ivanov",
    "repo": "session_1",
    "clone_url": "http://gitea:3000/candidate_ivan_ivanov/session_1.git",
    "web_url": "http://localhost:3000/candidate_ivan_ivanov/session_1"
  }
}
```

### 5. Улучшения БД

**Новые поля:**
- `access_token` - уникальный токен для кандидата
- `reviewer_token` - токен для проверяющего
- `candidate_name` - имя кандидата
- `reviewer_name` - имя проверяющего
- `status` - статус сессии ('active', 'completed', 'expired')
- `gitea_user` - имя пользователя в Gitea
- `gitea_repo` - имя репозитория в Gitea
- `gitea_pr_id` - ID Pull Request
- `gitea_enabled` - флаг активности Gitea интеграции

### 6. Улучшения безопасности

- Уникальные токены для каждого кандидата
- Разделение API endpoints по ролям
- Кандидат не имеет доступа к отчётам
- Кандидат не может продлевать сессию

## 🔄 Изменения в интерфейсе

### Главная страница теперь редиректит на `/reviewer`

**Было:**
- `http://localhost:8000/` → старый интерфейс

**Стало:**
- `http://localhost:8000/` → автоматический редирект на `/reviewer`
- `http://localhost:8000/reviewer` → новый интерфейс проверяющего
- `http://localhost:8000/candidate/:token` → интерфейс кандидата
- `http://localhost:8000/legacy` → старый интерфейс (для обратной совместимости)

## 📋 Как протестировать разделение ролей

### Шаг 1: Открыть Reviewer Dashboard

1. Откройте браузер: `http://localhost:8000/reviewer`
2. Вы увидите новый интерфейс со списком сессий

### Шаг 2: Создать сессию

1. Нажмите "Создать сессию"
2. Введите имя кандидата: "Иван Иванов"
3. Нажмите "Создать"
4. **Скопируйте токен** из ответа (или ссылку)

### Шаг 3: Открыть интерфейс кандидата (в другом браузере/окне)

1. Откройте в другом браузере или инкогнито: `http://localhost:8000/candidate/{token}`
2. Вы увидите интерфейс кандидата:
   - Diff кода
   - Возможность добавлять комментарии
   - Таймер сессии
   - **НЕТ** кнопок для отчётов и продления

### Шаг 4: Вернуться к Reviewer

1. Вернитесь в браузер Reviewer: `http://localhost:8000/reviewer`
2. Откройте созданную сессию
3. Вы увидите комментарии кандидата
4. Можете запустить оценку и просмотреть отчёт

## 🎯 Основные отличия

| Функция | Старый интерфейс (`/legacy`) | Новый Reviewer (`/reviewer`) | Candidate (`/candidate/:token`) |
|---------|------------------------------|------------------------------|--------------------------------|
| Создание сессий | ✅ | ✅ | ❌ |
| Просмотр списка сессий | ❌ | ✅ | ❌ |
| Просмотр diff | ✅ | ✅ | ✅ |
| Добавление комментариев | ✅ | ✅ | ✅ |
| Запуск оценки | ✅ | ✅ | ❌ |
| Просмотр отчётов | ✅ | ✅ | ❌ |
| Продление сессии | ✅ | ✅ | ❌ |
| Доступ по токену | ❌ | ❌ | ✅ |
| Gitea интеграция | ❌ | ✅ | ❌ |

## 🚀 Что дальше

1. **Протестируйте разделение ролей:**
   - Откройте `/reviewer` - создайте сессию
   - Откройте `/candidate/:token` в другом браузере - добавьте комментарии
   - Вернитесь в `/reviewer` - проверьте комментарии и отчёт

2. **Настройте Gitea (опционально):**
   - Откройте `http://localhost:3000`
   - Выполните первоначальную настройку
   - Создайте API токен
   - Установите `GITEA_ADMIN_TOKEN`

3. **Проверьте новую функциональность:**
   - Создание сессии с именем кандидата
   - Генерация токена
   - Разные интерфейсы для разных ролей

