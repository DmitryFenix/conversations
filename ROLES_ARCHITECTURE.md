# Архитектура разделения ролей: Reviewer (Проверяющий) и Candidate (Кандидат)

## 🎯 Концепция

### Reviewer (Проверяющий)
- ✅ Создает сессии для кандидатов
- ✅ Загружает MR (Merge Request) в .zip
- ✅ Видит все комментарии кандидата
- ✅ Генерирует и просматривает отчёты (текст + PDF)
- ✅ Может продлевать сессии
- ✅ Видит список всех созданных сессий
- ✅ Может оценивать работу кандидата

### Candidate (Кандидат)
- ✅ Получает ссылку на сессию (по session_id или токену)
- ✅ Видит diff кода
- ✅ Может добавлять комментарии к строкам
- ✅ Видит таймер сессии
- ❌ НЕ видит отчёты
- ❌ НЕ может создавать сессии
- ❌ НЕ может продлевать сессию
- ❌ НЕ может оценивать

---

## 📋 Варианты реализации

### Вариант 1: Режим переключения в UI (Рекомендуется для MVP)

**Преимущества:**
- Быстро реализовать
- Один кодбаза
- Легко тестировать

**Реализация:**
- Переключатель роли в UI (Reviewer/Candidate)
- Условный рендеринг компонентов
- Разные права доступа к API

**Тестирование:**
- Открыть два окна браузера
- В одном включить режим Reviewer, в другом - Candidate
- Или использовать режим инкогнито

---

### Вариант 2: Разные URL/роуты

**Преимущества:**
- Четкое разделение
- Можно использовать разные домены/поддомены
- Легко развернуть отдельно

**Реализация:**
- `/reviewer` - интерфейс проверяющего
- `/candidate/:sessionId` или `/candidate/:token` - интерфейс кандидата
- Разные компоненты/страницы

**Тестирование:**
- Открыть `http://localhost:8000/reviewer` в одном окне
- Открыть `http://localhost:8000/candidate/123` в другом

---

### Вариант 3: Система токенов/паролей

**Преимущества:**
- Безопасность
- Кандидат не может увидеть чужие сессии
- Можно отправлять ссылки с токенами

**Реализация:**
- При создании сессии генерируется токен
- Кандидат входит по токену
- Reviewer входит по паролю/секретному ключу

**Тестирование:**
- Reviewer создает сессию → получает токен
- Candidate открывает `/candidate?token=xxx`
- Разные браузеры/окна

---

### Вариант 4: Разные приложения (Полное разделение)

**Преимущества:**
- Максимальное разделение
- Можно развернуть на разных серверах
- Разные команды могут разрабатывать

**Реализация:**
- `reviewer-app/` - отдельное React приложение
- `candidate-app/` - отдельное React приложение
- Общий API бэкенд

**Тестирование:**
- Разные порты: `:8000` для reviewer, `:8001` для candidate
- Или разные домены

---

## 🏗️ Рекомендуемая архитектура (Вариант 1 + элементы Варианта 2)

### Структура БД

```sql
-- Добавить в sessions
ALTER TABLE sessions ADD COLUMN role TEXT DEFAULT 'candidate'; -- 'reviewer' | 'candidate'
ALTER TABLE sessions ADD COLUMN access_token TEXT; -- для доступа кандидата
ALTER TABLE sessions ADD COLUMN reviewer_id TEXT; -- кто создал сессию
ALTER TABLE sessions ADD COLUMN is_active BOOLEAN DEFAULT 1; -- активна ли сессия
```

### API Endpoints

#### Reviewer endpoints:
- `POST /api/reviewer/sessions` - создать сессию
- `GET /api/reviewer/sessions` - список всех сессий
- `GET /api/reviewer/sessions/{id}` - детали сессии
- `POST /api/reviewer/sessions/{id}/evaluate` - запустить оценку
- `GET /api/reviewer/sessions/{id}/report/pdf` - получить PDF
- `POST /api/reviewer/sessions/{id}/extend` - продлить сессию

#### Candidate endpoints:
- `GET /api/candidate/sessions/{token}` - получить сессию по токену
- `GET /api/candidate/sessions/{token}/diff` - получить diff
- `POST /api/candidate/sessions/{token}/comments` - добавить комментарий
- `GET /api/candidate/sessions/{token}` - получить информацию о сессии

### Frontend структура

```
frontend/src/
  ├── App.jsx (роутинг)
  ├── reviewer/
  │   ├── ReviewerDashboard.jsx
  │   ├── SessionList.jsx
  │   ├── SessionDetails.jsx
  │   └── ReportViewer.jsx
  ├── candidate/
  │   ├── CandidateView.jsx
  │   ├── CodeReview.jsx
  │   └── CommentForm.jsx
  └── shared/
      ├── Timer.jsx
      ├── MonacoEditor.jsx
      └── components/
```

---

## 🧪 План тестирования

### Сценарий 1: Режим переключения в UI

1. **Открыть приложение в двух окнах браузера**
   - Окно 1: `http://localhost:8000` → переключить на "Reviewer"
   - Окно 2: `http://localhost:8000` → оставить "Candidate"

2. **В окне Reviewer:**
   - Создать сессию для кандидата "Иван Иванов"
   - Загрузить MR в .zip
   - Получить session_id (например, 15)

3. **В окне Candidate:**
   - Ввести session_id: 15
   - Нажать "Присоединиться к сессии"
   - Увидеть diff
   - Добавить комментарии

4. **Вернуться в окно Reviewer:**
   - Увидеть комментарии кандидата
   - Нажать "Оценить"
   - Увидеть отчёт
   - Скачать PDF

### Сценарий 2: Разные URL

1. **Reviewer:**
   - Открыть `http://localhost:8000/reviewer`
   - Создать сессию → получить токен: `abc123xyz`

2. **Candidate:**
   - Открыть `http://localhost:8000/candidate/abc123xyz`
   - Автоматически загружается сессия
   - Может работать с кодом

3. **Reviewer:**
   - Видит активность кандидата в реальном времени
   - Может оценить работу

### Сценарий 3: Инкогнито режим

1. Открыть обычное окно → режим Reviewer
2. Открыть инкогнито → режим Candidate
3. Тестировать взаимодействие

---

## 🔐 Безопасность (для будущего)

1. **Токены доступа:**
   - Генерировать UUID для каждой сессии
   - Кандидат может войти только по токену
   - Токен в URL или localStorage

2. **Пароль для Reviewer:**
   - Простой пароль в .env
   - Или JWT токены
   - Или OAuth (Google/GitHub)

3. **Ограничения:**
   - Candidate не может видеть отчёты
   - Candidate не может продлевать сессию
   - Candidate не может видеть другие сессии

---

## 📝 Пример реализации (Минимальный MVP)

### 1. Добавить переключатель роли в UI

```jsx
const [role, setRole] = useState('candidate'); // 'reviewer' | 'candidate'

// В UI
<select value={role} onChange={e => setRole(e.target.value)}>
  <option value="candidate">Кандидат</option>
  <option value="reviewer">Проверяющий</option>
</select>
```

### 2. Условный рендеринг

```jsx
{role === 'reviewer' ? (
  <ReviewerInterface />
) : (
  <CandidateInterface />
)}
```

### 3. Разные API вызовы

```jsx
// Reviewer может создавать сессии
{role === 'reviewer' && (
  <button onClick={createSession}>Создать сессию</button>
)}

// Candidate может только присоединиться
{role === 'candidate' && (
  <input 
    placeholder="Введите session_id" 
    onChange={e => setSessionId(e.target.value)}
  />
)}
```

---

## 🚀 Поэтапный план внедрения

### Этап 1: Минимальный MVP (1-2 дня)
- [ ] Добавить переключатель роли в UI
- [ ] Скрыть кнопки "Оценить" и "PDF" для Candidate
- [ ] Скрыть кнопку "Создать сессию" для Candidate
- [ ] Тестирование в двух окнах

### Этап 2: Улучшения (2-3 дня)
- [ ] Добавить токены доступа
- [ ] Разные URL роуты
- [ ] Список сессий для Reviewer
- [ ] Улучшенный UI для каждой роли

### Этап 3: Безопасность (опционально)
- [ ] Пароль для Reviewer
- [ ] JWT токены
- [ ] Ограничения доступа на уровне API

---

## 💡 Рекомендация

**Начать с Варианта 1 (переключатель роли)** - это самый быстрый способ разделить логику и протестировать оба сценария использования.

Затем можно перейти к Варианту 2 (разные URL) для более четкого разделения.




