# 🚀 Настройка GitHub репозитория для Railway

## Быстрая инструкция

### Шаг 1: Запустите скрипт настройки

**Windows (PowerShell):**
```powershell
.\setup-github.ps1
```

**Mac/Linux:**
```bash
# Сначала создайте аналогичный скрипт или выполните команды вручную
```

### Шаг 2: Создайте репозиторий на GitHub

1. Зайдите на [github.com/new](https://github.com/new)
2. Введите название репозитория (например: `code-review-platform`)
3. **Важно:** НЕ добавляйте README, .gitignore или лицензию — они уже есть в проекте
4. Нажмите "Create repository"

### Шаг 3: Подключите remote и запушьте код

Замените `YOUR_USERNAME` и `YOUR_REPO_NAME` на ваши данные:

```bash
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO_NAME.git
git branch -M main
git push -u origin main
```

**Пример:**
```bash
git remote add origin https://github.com/username/code-review-platform.git
git branch -M main
git push -u origin main
```

### Шаг 4: Разверните на Railway

1. Зайдите на [railway.app](https://railway.app)
2. Нажмите "New Project"
3. Выберите "Deploy from GitHub repo"
4. Выберите ваш репозиторий
5. Railway автоматически развернет приложение!

---

## Если скрипт не работает

Выполните команды вручную:

```bash
# 1. Инициализация (если еще не инициализирован)
git init

# 2. Добавление всех файлов
git add .

# 3. Создание коммита
git commit -m "Initial commit: Code Review Platform"

# 4. Подключение remote (замените на ваш URL)
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO_NAME.git

# 5. Переименование ветки в main
git branch -M main

# 6. Пуш в GitHub
git push -u origin main
```

---

## Что будет закоммичено?

Скрипт добавит все файлы проекта, кроме тех, что в `.gitignore`:
- ✅ Весь исходный код
- ✅ Docker конфигурации
- ✅ Документация
- ✅ Скрипты развертывания

**Исключено:**
- ❌ `node_modules/`
- ❌ `__pycache__/`
- ❌ `.env` файлы
- ❌ Базы данных (`.db` файлы)
- ❌ `artifacts/` и временные файлы

---

## Проблемы?

**"git: command not found"**
- Установите Git: https://git-scm.com/

**"Permission denied"**
- Проверьте права доступа к репозиторию на GitHub
- Убедитесь что вы авторизованы: `git config --global user.name "Your Name"`

**"Repository not found"**
- Проверьте правильность URL репозитория
- Убедитесь что репозиторий создан на GitHub

---

*Готово к развертыванию!* 🎉

