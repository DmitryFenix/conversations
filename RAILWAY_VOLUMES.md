# Настройка Volumes в Railway

## Проблема

Railway не поддерживает `VOLUME` в Dockerfile. Вместо этого нужно использовать Railway Volumes.

## Решение

### Вариант 1: Без volumes (для начала)

Приложение будет работать, но данные не будут сохраняться между перезапусками. Это нормально для демо.

**Ничего делать не нужно** — Dockerfile уже исправлен.

### Вариант 2: Добавить Railway Volumes (для production)

После развертывания:

1. Зайдите в настройки проекта Railway
2. Перейдите в "Volumes"
3. Добавьте volumes:
   - `/artifacts` — для отчетов и diff файлов
   - `/mr_packages` — для MR packages

**Или через Railway CLI:**
```bash
railway volume create artifacts --mount /artifacts
railway volume create mr_packages --mount /mr_packages
```

---

## Что изменилось

- ✅ Удалена строка `VOLUME /artifacts /mr_packages` из Dockerfile
- ✅ Директории все еще создаются (`mkdir -p`)
- ✅ Приложение будет работать без volumes (для демо это нормально)

---

*Для production можно добавить volumes через настройки Railway.* 📦

