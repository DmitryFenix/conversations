# Выбор: Внешний Gitea vs Docker контейнер

## 📊 Сравнение вариантов

### Вариант 1: Внешний Gitea (на хосте, порт 4000)

#### ✅ Плюсы:
- **Уже настроен** - не нужно настраивать заново
- **Данные сохранены** - все репозитории и пользователи уже есть
- **Не занимает Docker ресурсы** - работает отдельно
- **Проще управлять отдельно** - можно перезапускать независимо
- **Прямой доступ** - легко открыть в браузере

#### ❌ Минусы:
- **Проблемы с доступом из контейнера**:
  - В WSL может не работать `host.docker.internal`
  - Нужно настраивать IP адрес хоста
  - Может быть сложнее в разных окружениях
- **Зависимость от внешнего сервиса**:
  - Если Gitea упадёт, нужно запускать отдельно
  - Не видно статуса в `docker compose ps`
- **Конфликт портов**:
  - Порт 4000 уже занят
  - Нельзя использовать стандартную конфигурацию
- **Менее портабельно**:
  - На другом сервере нужно настраивать Gitea отдельно
  - Сложнее развернуть всё одной командой

---

### Вариант 2: Gitea в Docker контейнере

#### ✅ Плюсы:
- **Всё в одном месте** - один `docker compose up` запускает всё
- **Простое управление** - `docker compose start/stop/restart`
- **Нет проблем с доступом** - внутри Docker сети всё работает
- **Портабельность** - легко перенести на другой сервер
- **Изоляция** - не конфликтует с другими сервисами
- **Консистентность** - все сервисы в одном окружении
- **Легко обновлять** - просто обновить образ
- **Можно использовать другой порт** - например, 4001:4000

#### ❌ Минусы:
- **Нужно настраивать заново** (если есть данные):
  - Создать администратора
  - Создать API токен
  - Возможно, мигрировать данные
- **Занимает порт** (но можно использовать другой внешний порт)
- **Больше ресурсов Docker** (но Gitea лёгкий)

---

## 🎯 Рекомендация

### Для разработки и production: **Gitea в Docker** ✅

**Почему:**
1. **Консистентность** - все сервисы в одном месте
2. **Простота развёртывания** - `docker compose up` запускает всё
3. **Нет проблем с доступом** - всё работает внутри Docker сети
4. **Портабельность** - легко перенести на другой сервер
5. **Легче поддерживать** - всё в одном месте

### Когда использовать внешний Gitea:
- Если у вас уже есть production Gitea с важными данными
- Если Gitea используется другими проектами
- Если нужен централизованный Git сервер

---

## 🔧 Реализация: Gitea в Docker

### Вариант A: Использовать другой внешний порт (рекомендуется)

**Преимущества:**
- Не конфликтует с существующим Gitea на 4000
- Можно использовать оба одновременно
- Легко переключиться обратно

**Конфигурация:**
```yaml
gitea:
  ports:
    - "4001:4000"  # Внешний порт 4001, внутренний 4000
  environment:
    - GITEA__server__HTTP_PORT=4000
    - GITEA__server__ROOT_URL=http://localhost:4001
```

**Доступ:**
- Из браузера: `http://localhost:4001`
- Из контейнера API: `http://gitea:4000`

### Вариант B: Остановить внешний Gitea и использовать порт 4000

**Преимущества:**
- Использует стандартный порт
- Не нужно менять URL в браузере

**Недостатки:**
- Нужно остановить внешний Gitea
- Потеряете доступ к существующим данным (если не мигрируете)

---

## 📋 План миграции (если есть данные)

Если у вас есть важные данные во внешнем Gitea:

1. **Экспорт данных** (если нужно):
   ```bash
   # Бэкап базы данных Gitea
   cp /path/to/gitea/data/gitea.db ./gitea_data/gitea.db
   ```

2. **Или начните с чистого состояния**:
   - Создайте нового администратора
   - Создайте API токен
   - Старые данные останутся во внешнем Gitea

---

## 🚀 Рекомендуемая конфигурация

### Использовать порт 4001 для Docker Gitea

Это позволит:
- ✅ Запускать Gitea в Docker
- ✅ Не конфликтовать с внешним Gitea на 4000
- ✅ Легко переключаться между ними
- ✅ Использовать стандартную Docker сеть

**Что нужно сделать:**
1. Раскомментировать сервис `gitea` в `docker-compose.yml`
2. Изменить порт на `4001:4000`
3. Обновить `GITEA_URL` на `http://gitea:4000` (внутри Docker)
4. Обновить `GITEA__server__ROOT_URL` на `http://localhost:4001`
5. Создать `.env` с токеном после настройки

---

## 💡 Итоговая рекомендация

**Используйте Gitea в Docker на порту 4001** - это даст вам:
- Простоту управления
- Отсутствие проблем с доступом
- Портабельность
- Возможность использовать оба Gitea одновременно

Хотите, чтобы я настроил это?

